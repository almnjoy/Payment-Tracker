Context:
I already added OPENAI_API_KEY in Replit Secrets.
I created server/services/openaiClient.ts:

import OpenAI from "openai";
export const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });
export function isLlmEnabled() {
  return Boolean(process.env.OPENAI_API_KEY) && process.env.AI_ANALYZER_ENABLE_LLM !== "false";
}

Frontend page already exists and calls:
POST /api/admin/ai-finance/analyze
with { query, days? } and expects this shape:

interface RecurringPayment {
  merchantName: string;
  frequency: string;
  averageAmount: number; // cents
  estimatedMonthlyCost: number; // cents
  transactionCount: number;
  lastDate: string;
  confidence: string; // high|medium|low
}
interface AnalysisResult {
  summary: string;
  recurringPayments: RecurringPayment[];
  totalMonthlyEstimate: number; // cents
  analyzedTransactions: number;
  dateRange: { start: string; end: string };
}

TASK:
Implement the backend endpoint POST /api/admin/ai-finance/analyze so the existing UI works.
Do this carefully without breaking existing routing.

Requirements:
1) Locate how routes are registered (Express server likely in server/index.ts or similar). Add the new route in the existing router pattern used in the app (do NOT randomly create a new server).
2) Endpoint: POST /api/admin/ai-finance/analyze
   - Use admin auth middleware consistent with the rest of the /api/admin routes (same as Plaid admin endpoints).
   - Read JSON body: { query: string, days?: number, useLLM?: boolean }.
   - Default days=90, useLLM=false for now (make LLM optional and safe).
3) Data source:
   - Pull Plaid transactions already stored/synced in our DB for the logged-in admin/user.
   - Use the same DB models/utilities used by Finance Tracker / Plaid pages.
   - Analyze only the last N days (days param).
   - Count analyzedTransactions.
   - Determine dateRange.start/end based on the returned transaction set.
4) Heuristic recurring detection:
   - Group by normalized merchant/name (lowercase, trim, collapse spaces; optionally strip punctuation).
   - Only consider groups with 2+ transactions.
   - Sort dates, compute average day interval between consecutive txns.
   - Frequency classification:
     6–8 => weekly (monthly multiplier 4.33, confidence high)
     13–16 => biweekly (multiplier 2.17, confidence high)
     25–35 => monthly (multiplier 1.0, confidence high)
     350–380 => yearly (monthly multiplier 1/12, confidence medium)
     otherwise skip
   - For each recurring merchant output:
     merchantName (use a cleaned display name, but keep readable)
     frequency
     averageAmount (average absolute amount in cents; use positive cents for display)
     estimatedMonthlyCost (averageAmount * multiplier, rounded to nearest cent)
     transactionCount
     lastDate (most recent transaction date, ISO yyyy-mm-dd)
     confidence
   - Sort results by estimatedMonthlyCost descending.
   - totalMonthlyEstimate = sum of estimatedMonthlyCost
5) summary string:
   - If recurringPayments is empty: “No recurring payments detected…”
   - Else: a short summary like:
     “Detected X recurring payments with an estimated monthly total of $Y.”
   NOTE: Keep values in cents in API response.
6) Optional LLM add-on (but do not require it):
   - If request.body.useLLM === true AND OPENAI_API_KEY exists AND isLlmEnabled() then generate a more human summary.
   - Do NOT send raw transaction descriptions if avoidable; send only the recurringPayments array + query.
   - If LLM fails, fall back to heuristic summary and still return 200.
7) Stability:
   - Add good error handling and server logs (console.error with context).
   - If DB returns 0 transactions, return 200 with empty results, not 500.
8) Deliverables:
   - Identify files changed and why.
   - Provide a quick curl example for local test.
   - Confirm the endpoint returns exactly the AnalysisResult shape expected by the UI.

IMPORTANT:
- DO NOT change the existing frontend page unless strictly necessary for a naming mismatch.
- If there is a mismatch in field names (merchantName vs merchant, etc.), fix it in the backend to match the UI’s expected shape.
- Keep the route under /api/admin consistent with the rest of the codebase.

Start by finding the existing Plaid admin routes (like /api/admin/plaid/sync) and mirror the same structure/auth pattern for this new route.